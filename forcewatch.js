(function($) {
	$.fn.forcewatch = function(options) {
		var settings = $.extend({
			'duration': .9,
			'callback': function() {}
		}, options);

		return this.each(function() {
			
		});
	};
})(jQuery);